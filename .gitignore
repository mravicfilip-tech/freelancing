node_modules
dist
.DS_Store
*.log
remittix-hero.html
.vercel
.env*
# Regenerated by scripts/mobile-showcase.mjs; 1.6MB of embedded captures, published rather than committed.
screenshots/mobile-showcase.html

# Scratch probes: throwaway measurement scripts written while diagnosing a scene.
# They are deleted when the diagnosis lands; this keeps one from being committed by accident.
.probe*
.chestshot*
.chestout/
y*.mjs
