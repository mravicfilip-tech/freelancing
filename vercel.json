{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "//": "Single page: everything is index.html — except the asset trees, which must 404 as themselves rather than come back as a 200 page body. /fonts/ is where the licensed display face is expected to land (see FigmaHero.css).",
      "source": "/((?!assets/|figma/|fonts/).*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "//": "Vite fingerprints these, so a changed file is a changed URL and the old one can be kept forever.",
      "source": "/assets/(.*)",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }]
    },
    {
      "//": "The design's own artwork: not fingerprinted, so it is cached for a day and revalidated after.",
      "source": "/figma/(.*)",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=86400, stale-while-revalidate=604800" }]
    },
    {
      "source": "/(favicon.svg|favicon-32.png|apple-touch-icon.png|land-mask.png|hero-planet-fallback.png)",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=86400, stale-while-revalidate=604800" }]
    }
  ]
}
